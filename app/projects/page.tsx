import Link from "next/link";
import { OSShell } from "@/components/os/os-shell";
import { OSWindow } from "@/components/os/window";
import { portfolio } from "@/config/portfolio.config";
export default function ProjectsPage(){return <OSShell><div className="mx-auto max-w-6xl px-4 pb-28 sm:px-6 lg:px-10"><OSWindow title="projects.all"><h1 className="text-4xl font-semibold tracking-tight">Project</h1><p className="mt-3 text-muted">Kumpulan pekerjaan terpilih dengan konteks masalah, keputusan desain, dan dampak teknis.</p><div className="mt-8 grid gap-5">{portfolio.projects.map(p=><Link key={p.slug} href={`/projects/${p.slug}`} className="rounded-3xl border border-border bg-card p-6 transition hover:border-accent/40"><div className="flex flex-wrap items-start justify-between gap-4"><div><h2 className="text-2xl font-semibold">{p.title}</h2><p className="mt-2 max-w-3xl leading-7 text-muted">{p.overview}</p></div><span className="text-sm text-muted">{p.year}</span></div><div className="mt-5 flex flex-wrap gap-2">{p.stack.map(s=><span key={s} className="rounded-full border border-border px-3 py-1 text-xs text-muted">{s}</span>)}</div></Link>)}</div></OSWindow></div></OSShell>}

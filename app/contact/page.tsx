import { OSShell } from "@/components/os/os-shell";import { OSWindow } from "@/components/os/window";import { Button } from "@/components/ui/button";import { portfolio } from "@/config/portfolio.config";export default function Contact(){return <OSShell><div className="mx-auto max-w-3xl px-4 pb-28"><OSWindow title="contact.compose"><h1 className="text-4xl font-semibold">Kontak</h1><p className="mt-4 leading-7 text-muted">Kalau ada produk yang sedang dibangun, UI yang perlu dibereskan, atau sistem desain yang mulai terasa berat, kirim konteks singkat. {portfolio.contact.response}</p><Button asChild className="mt-8"><a href={portfolio.socials.email}>Kirim email ke {portfolio.contact.email}</a></Button></OSWindow></div></OSShell>}

import { OSShell } from "@/components/os/os-shell";import { OSWindow } from "@/components/os/window";import { portfolio } from "@/config/portfolio.config";export default function Resume(){return <OSShell><div className="mx-auto max-w-4xl px-4 pb-28"><OSWindow title="resume.pdf"><h1 className="text-4xl font-semibold">Resume</h1><p className="mt-3 text-muted">Ringkasan singkat profil profesional {portfolio.name}.</p><div className="mt-8 space-y-5">{portfolio.experience.map(e=><div key={e.company} className="rounded-3xl border border-border bg-card p-5"><p className="text-sm text-muted">{e.period}</p><h2 className="mt-1 font-semibold">{e.role} · {e.company}</h2><p className="mt-2 text-muted">{e.description}</p></div>)}</div></OSWindow></div></OSShell>}
